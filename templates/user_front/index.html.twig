{% extends 'base.html.twig' %}

{% block title %}STEG{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 0vh;
    }

    .example-wrapper2 {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 120vh;
    }

    body {
        background-image: url('/images/Capture1.png');
        width: 100%;
        height: 100vh;
        background-repeat: no-repeat;
        background-attachment: fixed;
    }

    .btn-container {
        text-align: center;
    }

    .btn-big {
        color: #fff;
        font-size: 24px;
        padding: 15px 30px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        margin: 200px 40px;
        position: relative;
    }

    .btn-big:hover {
        background-color: rgba(20, 173, 194, 0.6);
        transform: scale(1.05);
    }

    .btn-big2 {
        background-color: rgba(17, 112, 176, 0.6);
        color: #fff;
        font-size: 24px;
        padding: 15px 30px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        margin: 200px 40px;
        position: relative;
    }

    .btn-big2:hover {
        background-color: rgba(20, 173, 194, 0.6);
        transform: scale(1.05);
    }

    .tooltip {
        position: absolute;
        background-color: #333;
        color: #fff;
        padding: 10px;
        border-radius: 5px;
        width: 250px;
        text-align: center;
        top: -50px;
        left: 50%;
        transform: translateX(-50%);
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s, visibility 0.3s;
    }

    .btn-big:hover .tooltip {
        opacity: 1;
        visibility: visible;
    }
</style>

<div class="example-wrapper">
    <div class="btn-container">
        <a href="#" class="btn btn-big"><strong>Qui sommes-nous ?</strong><span class="tooltip">La Société Tunisienne de l'Électricité et du Gaz (STEG) est l'entreprise nationale de Tunisie responsable de la production, de la distribution et de la commercialisation de l'électricité et du gaz. Elle joue un rôle crucial dans la gestion des infrastructures énergétiques du pays, assurant ainsi l'approvisionnement en énergie électrique et en gaz naturel pour les besoins des ménages, des entreprises et de l'industrie tunisienne</span></a>
    </div>
</div>


<div class="example-wrapper2">
    <div class="btn-container">
        {% if app.user %}
            <a href="{{ path('app_user_front_stage') }}" class="btn btn-big2">Trouver un Stage</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-big2">Trouver un Stage</a>
        {% endif %}

    </div>
</div>


{% endblock %}
